 - [x] install vagrant
 - [x] create Virtual Machine Using Vagrantfile
 - [x] connect to the VM using ``` vagrant ssh ```
 - [x] install ansible in VM
```bash
$ sudo apt update
$ sudo apt install software-properties-common
$ sudo add-apt-repository --yes --update ppa:ansible/ansible
$ sudo apt install ansible
```